#include<stdio.h>
#include<string.h>
#include<stdlib.h>

void generateBillFooter(float total){
  return cgst=0.09*netTotal;
  return grandTotal=netTotal + 2*cgst;
  return dis=0.1*total;
  return netTotal=total-dis;
}
